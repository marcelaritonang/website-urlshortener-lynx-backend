# Railway auto-populated variables (don't change these names):
DATABASE_URL=postgresql://user:password@host:5432/dbname
REDIS_URL=redis://host:6379

# Manual configuration (set in Railway dashboard):
APP_ENV=production
GIN_MODE=release
JWT_SECRET=your-secret-key-minimum-32-characters-long-please
BASE_URL=https://your-app.railway.app
PORT=8080

# Optional (Railway auto-fills from DATABASE_URL):
DB_HOST=
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=

# Optional (Railway auto-fills from REDIS_URL):
REDIS_HOST=
REDIS_PORT=6379
REDIS_PASSWORD=
